# O painel /admin (Decap) só funciona em ambiente local:
#   npm run admin:local  +  npm run dev  ->  http://localhost:4321/admin/
# Em produção qualquer acesso a /admin é enviado para a home, para não deixar
# uma página morta exposta. Para reativar o painel no site, apague as 2 linhas.
/admin      /   302
/admin/*    /   302
